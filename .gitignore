# Sentinel runtime output: videos, screenshots, traces, saved sessions, graphs
.sentinel/

# Local credentials / config
.env
.env.local

# Private first-party adapter overlay (not part of the open-source surface)
src/adapters/private/

# Build / cache
dist/
.turbo/
*.tsbuildinfo
node_modules/
.DS_Store
